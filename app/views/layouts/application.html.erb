<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title><%= content_for(:title) || 'News' %></title>
    <% if controller_name == 'search' && action_name == 'index' %>
      <%= auto_discovery_link_tag(:atom, search_url(format: :atom, q: params[:q]), title: content_for(:title)) %>
    <% end %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://unpkg.com/tachyons@4.10.0/css/tachyons.min.css"/>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body id="<%= content_for(:page) %>" class="flex flex-column min-vh-100">

    <% #unless content_for(:page) == 'home' %>
      <%= render 'layouts/navbar' %>
    <% #end %>

    <div class="center w-100">
      <%= yield %>
    </div>

    <footer class="mt2">
      <div class="ph4">
        <p class="text-center text-muted">
          <span>Forked by <a href="https://orph.eu" class="text-muted"><strong>mario</strong></a>; built by <a href="https://vinc.cc" class="text-muted"><strong>vinc</strong></a>.</span>
          <span>Code licensed <a href="https://github.com/vinc/news/blob/master/LICENSE" class="text-muted"><strong>MIT</strong></a>.</span>
        </p>
      <div>
    </footer>
  </body>
</html>
